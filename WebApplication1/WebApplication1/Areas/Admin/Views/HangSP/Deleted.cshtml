@model PetaPoco.Page<MobileShopConnection.HangSP>

@{
    ViewBag.Title = "Deleted";
    Layout = "~/Areas/Admin/Views/Shared/_AdminMainLayout.cshtml";
}

<div class="container-fluid">
    <!-- Page Heading -->
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">
                Hãng Sản Phẩm Đã Xóa
            </h1>
            <ol class="breadcrumb">
                <li>
                    <i class="fa fa-dashboard"></i>  <a href="@Url.Action("Index","HangSP")">Hãng Sản Phẩm</a>
                </li>
                <li class="active">
                    <i class="fa fa-file"></i> Hãng Sản Phẩm Đã Xóa
                </li>
            </ol>
            <div class="table-responsive">
                <table class="table table-bordered table-hover">
                    <tr>
                        <th>
                            Mã Hãng
                        </th>
                        <th>
                            Tên Hãng
                        </th>
                        <th></th>
                    </tr>

                    @foreach (var item in Model.Items)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.MaHang)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TenHang)
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.MaHang }) |
                                @Html.ActionLink("Restore", "Restore", new { id = item.MaHang })
                            </td>
                        </tr>
                    }

                </table>
            </div>
            <div class="text-center">
                <ul class="pagination">
                    @{
                        if (Model.CurrentPage > 1)
                        {
                            <li>
                                <a href="@Url.Action("Index","HangSP",new { Page = Model.CurrentPage - 1})" aria-label="Previous">
                                    <span aria-hidden="true">&laquo;</span>
                                </a>
                            </li>
                        }
                    }
                    @for (int i = 1; i <= Model.TotalPages; i++)
                    {
                        if (i == Model.CurrentPage)
                        {
                            <li class="active">@Html.ActionLink(i.ToString(), "Index", new { Page = i })</li>
                        }
                        else
                        {
                            <li>@Html.ActionLink(i.ToString(), "Index", new { Page = i })</li>
                        }
                    }
                    @{
                        if (Model.CurrentPage < Model.TotalPages)
                        {
                            <li>
                                <a href="@Url.Action("Index","HangSP",new { Page = Model.CurrentPage + 1})" aria-label="Next">
                                    <span aria-hidden="true">&raquo;</span>
                                </a>
                            </li>
                        }
                    }
                </ul>
            </div>
        </div>
    <!-- /.row -->
    </div>
</div>

